# --- Web Framework & API ---# For the Gradio UI, FastAPI backend, and communicationgradiofastapiuvicorn[standard] # Includes standard dependencies like python-multipart for file uploadsrequests# Note: conversion to Django is in process# --- Core ML & AI (PyTorch & Hugging Face) ---# Note: For GPU support, install the CUDA-enabled version of torch# See: https://pytorch.org/get-started/locally/torchtransformerspeft              # For using LoRA fine-tuned modelsaccelerate        # Often required by transformers for device mappingbitsandbytes      # For 4-bit model quantization (BitsAndBytesConfig)# --- Speech, NLP, & Language Models ---whisper-timestamped # A fork of Whisper that provides word-level timestampslangchainlangchain-corelangchain-huggingface# --- Audio Processing ---demucs            # For separating vocals from instrumentalspydub             # For audio manipulation (silencing segments)mutagen           # For reading and writing audio metadata (ID3 tags)# --- Utilities & Data Handling ---numpylyricsgenius      # For fetching lyrics from Genius.comjiwer             # For calculating Word Error Rate (WER/MER)